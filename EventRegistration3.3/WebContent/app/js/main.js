$(function() {
	$("#navLoggedIn").load("../components/navLoggedIn.html");
	$("#navNotLoggedIn").load("../components/navNotLoggedIn.html");
	$("#header").load("../components/header.html");
	$("#footer").load("../components/footer.html");
});

function validateForm() {
	
	var email = window.document.forms.registerForm.elements["email"].value;
	var username = window.document.forms.registerForm.elements["username"].value;
	var username = window.document.forms.registerForm.elements["username"].value;
	var username = window.document.forms.registerForm.elements["password"].value;
	var username = window.document.forms.registerForm.elements[""].value;

}


function checkEmail(email) {
	if(new RegExp() ) {
		
	}
}